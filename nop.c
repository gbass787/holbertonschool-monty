#include "monty.h"

/**
 * nop-does nothing
 * @stack: pointer top stack
 * @line_count - line to read
 * Return: 0
 */
void nop(stack_t **stack, unsigned int line_count)
{
	(void) stack;
	(void) line_number;
}
